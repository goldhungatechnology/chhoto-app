[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
version = "0.1.0"
name = "chhoto_encoding"
description = "A Python package for encoding and decoding data using the Chhoto algorithm."

[tool.hatch.build.targets.wheel]
packages = ["src/chhoto_encoding"]
